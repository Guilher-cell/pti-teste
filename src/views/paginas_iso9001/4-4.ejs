<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FlowCerti - ISO 9001:2015 - 4.4 Sistema de gestão da qualidade e seus processos</title>

    <link rel="icon" type="image/png" href="Imagem/footer/logo_pequena.png">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&family=Sora:wght@100..800&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="/css/subcapitulo.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include("../includes/header", { user: user }) %>
    <ul class="breadcrumb">
  <li><a href="/">Início</a></li>
  <li>
    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#cccccc" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
  </li>
  <li><a href="/dashboard">Dashboard</a></li>
  <li>
    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#cccccc" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
  </li>
  <li><a href="/gerenciar">ISO 9001:2015</a></li>
  <li>
    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#cccccc" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
  </li>
  <li><a href="/iso9001/4">4. Contexto da Organização</a></li>
  <li>
    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#cccccc" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
  </li>
  <li class="current">4.4 Sistema de gestão da qualidade e seus processos</li>
</ul>

<!-- ===== Subcapítulo 4.4 ===== -->
<main class="subcap">
  <%- include('../includes/messages') %>

  <!-- Header -->
  <div class="subcap-header">
    <h2>4.4 Sistema de gestão da qualidade e seus processos</h2>
    <p>Envie o documento principal que comprove este requisito e utilize o checklist apenas como lembrete do conteúdo que ele deve cobrir.</p>
  </div>

  <!-- Orientação do documento -->
  <section class="subcap-hint">
    <button class="subcap-hint-toggle" aria-expanded="false">
      <span>Ver orientação do documento</span>
      <svg viewBox="0 0 24 24" class="chev" aria-hidden="true">
        <path d="M7 10l5 5 5-5"/>
      </svg>
    </button>
    <div class="subcap-hint-body" hidden>
      <p><strong>Documento recomendado:</strong> Mapa de processos, fluxogramas,
        descrição de processos <span class="muted">+ indicadores</span>.
      </p>
    </div>
  </section>

  <!-- Checklist -->
  <section class="subcap-checklist">
    <h3>Checklist do Documento</h3>
    <form action="/iso9001/<%= capitulo %>/checklist" method="POST">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">

      <ul>
        <li>
          <label class="chk">
            <input type="checkbox" name="chk_processos" class="chk-item" <%= documentos[0]?.checklist?.includes("chk_processos") ? "checked" : "" %>>
            <span class="checkmark"></span>
            <span>Lista de processos necessários</span>
          </label>
          <button type="button" class="more" data-target="s1">Ler mais</button>
          <div id="s1" class="more-body" hidden>
          Identifique todos os processos que compõem o SGQ,
          como vendas, produção, compras, RH, manutenção etc.
        </div>
        </li>

        <li>
          <label class="chk">
            <input type="checkbox" name="chk_entradas_saidas" class="chk-item" <%= documentos[0]?.checklist?.includes("chk_entradas_saidas") ? "checked" : "" %>>
            <span class="checkmark"></span>
            <span>Identificação de entradas e saídas de cada processo</span>
          </label>
          <button type="button" class="more" data-target="s2">Ler mais</button>
          <div id="s2" class="more-body" hidden>
          Para cada processo, registre o que ele precisa (entradas) e o que entrega (saídas).
          Exemplo: compras → entrada: requisição de materiais, saída: nota fiscal.
        </div>
        </li>

        <li>
          <label class="chk">
            <input type="checkbox" name="chk_sequencia" class="chk-item" <%= documentos[0]?.checklist?.includes("chk_sequencia") ? "checked" : "" %>>
            <span class="checkmark"></span>
            <span>Descrição da sequência e interação dos processos</span>
          </label>
          <button type="button" class="more" data-target="s3">Ler mais</button>
          <div id="s3" class="more-body" hidden>
          Mostre como os processos se relacionam entre si (um depende do outro).
          Use fluxograma ou mapa de processos para visualizar.
        </div>
        </li>

        <li>
          <label class="chk">
            <input type="checkbox" name="chk_indicadores" class="chk-item" <%= documentos[0]?.checklist?.includes("chk_indicadores") ? "checked" : "" %>>
            <span class="checkmark"></span>
            <span>Critérios/métodos de monitoramento e indicadores de desempenho</span>
          </label>
          <button type="button" class="more" data-target="s4">Ler mais</button>
          <div id="s4" class="more-body" hidden>
          Cada processo deve ter critérios e indicadores para medir sua eficácia.
          Exemplo: produção → taxa de retrabalho; vendas → satisfação do cliente.
        </div>
        </li>

        <li>
          <label class="chk">
            <input type="checkbox" name="chk_recursos" class="chk-item" <%= documentos[0]?.checklist?.includes("chk_recursos") ? "checked" : "" %>>
            <span class="checkmark"></span>
            <span>Recursos necessários definidos</span>
          </label>
          <button type="button" class="more" data-target="s5">Ler mais</button>
          <div id="s5" class="more-body" hidden>
          Registre os recursos que cada processo precisa (equipamentos, software, pessoas, infraestrutura).
        </div>
        </li>

        <li>
          <label class="chk">
            <input type="checkbox" name="chk_responsaveis" class="chk-item" <%= documentos[0]?.checklist?.includes("chk_responsaveis") ? "checked" : "" %>>
            <span class="checkmark"></span>
            <span>Responsabilidades e autoridades atribuídas</span>
          </label>
          <button type="button" class="more" data-target="s6">Ler mais</button>
          <div id="s6" class="more-body" hidden>
            Mostre quem é responsável por cada processo e quais decisões essa pessoa pode tomar.
          </div>
        </li>

        <li>
          <label class="chk">
            <input type="checkbox" name="chk_riscos" class="chk-item" <%= documentos[0]?.checklist?.includes("chk_riscos") ? "checked" : "" %>>
            <span class="checkmark"></span>
            <span>Identificação dos riscos e oportunidades</span>
          </label>
          <button type="button" class="more" data-target="s7">Ler mais</button>
          <div id="s7" class="more-body" hidden>
            Liste os riscos que podem prejudicar o processo e as oportunidades que podem trazer benefícios.
          </div>
        </li>

        <li>
          <label class="chk">
            <input type="checkbox" name="chk_melhorias" class="chk-item" <%= documentos[0]?.checklist?.includes("chk_melhorias") ? "checked" : "" %>>
            <span class="checkmark"></span>
            <span>Registros de melhoria dos processos</span>
          </label>
          <button type="button" class="more" data-target="s8">Ler mais</button>
          <div id="s8" class="more-body" hidden>
            Guarde evidências de melhorias feitas (planos de ação, indicadores antes/depois, atas de reunião).
          </div>
        </li>

        <li>
          <label class="chk">
            <input type="checkbox" name="chk_documentacao" class="chk-item" <%= documentos[0]?.checklist?.includes("chk_documentacao") ? "checked" : "" %>>
            <span class="checkmark"></span>
            <span>Documentação de apoio (procedimentos, instruções)</span>
          </label>
          <button type="button" class="more" data-target="s9">Ler mais</button>
          <div id="s9" class="more-body" hidden>
            Cada processo deve ter instruções claras de execução para garantir padronização e conformidade.
          </div>
        </li>
      </ul>

      <p class="subcap-note">* O checklist é apenas um lembrete. O documento enviado deve cobrir todos os pontos.</p>  

      <div class="form-actions">
        <button type="submit" id="btn-salvar-checklist" class="btn-subcap-salvar">Salvar Checklist</button>
      </div>
    </form>
  </section>


 <!-- Upload + Ações -->
<section class="subcap-upload">
  <form action="/iso9001/<%= capitulo %>/upload" method="POST" enctype="multipart/form-data" id="form-subcap">
    <input type="hidden" name="_csrf" value="<%= csrfToken %>">

    <div class="upload-actions">
      <!-- Botão de anexo -->
      <input type="file" id="file-upload" name="ficheiros[]" multiple hidden>
      <label for="file-upload" class="btn-anexo">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16"
             viewBox="0 0 24 24" fill="none" stroke="#ffffff" stroke-width="3"
             stroke-linecap="round" stroke-linejoin="round">
          <path d="M5 12h14"/><path d="M12 5v14"/>
        </svg>
        Adicionar anexo
      </label>

      <!-- Botões de ação -->
      <div class="form-actions">
        <button type="submit" id="btn-salvar" class="btn-subcap-salvar">Salvar</button>
        <button type="button" id="btn-cancelar" class="btn-subcap-cancelar" hidden>Cancelar</button>
      </div>
    </div>

    <!-- Pré-visualização -->
    <div id="file-preview" class="file-preview vazio">
      <em>Nenhum arquivo selecionado.</em>
    </div>
  </form>

  <!-- Arquivos já anexados -->
  <% if (documentos && documentos.length > 0) { %>
    <div class="subcap-arquivos">
      <% documentos.forEach(doc => { %>
        <% if (doc.ficheiros && doc.ficheiros.length > 0) { %>
          <% doc.ficheiros.forEach(f => { %>
            <div class="file-row">
              <!-- Nome personalizado ou original -->
              <span class="file-name">
                <a href="/uploads/<%= f.path %>" target="_blank">
                  <%= f.nomePersonalizado || f.nomeOriginal %>
                </a>
              </span>

              <!-- Tamanho -->
              <span class="file-size"><%= (f.size / 1024).toFixed(2) %> KB</span>

              <!-- Aprovado por -->
              <input type="text" value="<%= f.aprovadoPor || '' %>"
                     placeholder="Aprovado por..." readonly>

              <!-- Botão excluir -->
              <form action="/iso9001/<%= capitulo %>/ficheiro/<%= f._id %>/apagar"
                    method="POST" style="display:inline;">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <button type="submit" class="btn-apagar" title="Excluir arquivo">
                  <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24"
                       viewBox="0 0 24 24" fill="none" stroke="#ff4d4d" stroke-width="2"
                       stroke-linecap="round" stroke-linejoin="round"
                       class="lucide lucide-trash-2">
                    <path d="M10 11v6"/><path d="M14 11v6"/>
                    <path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6"/>
                    <path d="M3 6h18"/><path d="M8 6V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/>
                  </svg>
                </button>
              </form>
            </div>
          <% }) %>
        <% } %>
      <% }) %>
    </div>
  <% } %>
</section>


</main>
   <%- include('../includes/footer') %>
    
    <script src="/assets/js/subcapitulo.js"></script>
    <script src="/assets/js/script.js"></script>
</body>
</html>