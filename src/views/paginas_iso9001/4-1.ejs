<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FlowCerti - ISO 9001:2015 - 4.1 Entendendo a organização e seu contexto</title>

    <link rel="icon" type="image/png" href="Imagem/footer/logo_pequena.png">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&family=Sora:wght@100..800&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="/css/subcapitulo.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
   <%- include("../includes/header", { user: user }) %>

    <!-- Breadcrumb -->
        <ul class="breadcrumb">
  <li><a href="/">Início</a></li>
  <li>
    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#cccccc" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
  </li>
  <li><a href="/dashboard">Dashboard</a></li>
  <li>
    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#cccccc" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
  </li>
  <li><a href="/gerenciar">ISO 9001:2015</a></li>
  <li>
    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#cccccc" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
  </li>
  <li><a href="/iso9001/4">4. Contexto da Organização</a></li>
  <li>
    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#cccccc" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
  </li>
  <li class="current">4.1 Entendendo organização e seu contexto</li>
</ul>

<!-- ===== Subcapítulo 4.1 ===== -->
<main class="subcap">
  <!-- Mensagens -->
  <%- include('../includes/messages') %>

  <!-- Header -->
  <div class="subcap-header">
    <h2>4.1 Entendendo a organização e seu contexto</h2>
    <p>Envie o documento principal que comprove este requisito e utilize o checklist apenas como lembrete do conteúdo que ele deve cobrir.</p> 
  </div>

  <!-- Orientação do documento -->
  <section class="subcap-hint">
    <button class="subcap-hint-toggle" aria-expanded="false">
      <span>Ver orientação do documento</span>
      <svg viewBox="0 0 24 24" class="chev" aria-hidden="true"><path d="M7 10l5 5 5-5"/></svg>
    </button>
    <div class="subcap-hint-body" hidden>
      <p><strong>Documento recomendado:</strong> Relatório de análise de contexto
        <span class="muted">(pode ser SWOT, PESTEL ou relatório estratégico)</span>.
      </p>
    </div>
  </section>

  <!-- Checklist -->
  <section class="subcap-checklist">
    <h3>Checklist do Documento</h3>
    <form action="/iso9001/<%= capitulo %>/checklist" method="POST">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">

      <ul>
        <li>
          <label class="chk">
            <input type="checkbox" name="chk_interno" <%= documentos[0]?.checklist?.includes("chk_interno") ? "checked" : "" %>>
            <span class="checkmark"></span>
            <span>Descrição das questões internas (valores, cultura, desempenho, recursos, etc.)</span>
          </label>
          <button type="button" class="more" data-target="m1">Ler mais</button>
          <div id="m1" class="more-body" hidden>
          Você deve registrar fatores internos que influenciam o desempenho da empresa.
          Isso inclui a cultura organizacional (como a empresa se comporta), valores,
          nível de conhecimento, desempenho de processos, recursos financeiros e humanos.
          Esse levantamento ajuda a entender a realidade da empresa por dentro.
        </div>
        </li>
        <li>
          <label class="chk">
            <input type="checkbox" name="chk_externo" class="chk-item" <%= documentos[0]?.checklist?.includes("chk_externo") ? "checked" : "" %>>
            <span class="checkmark"></span>
            <span>Descrição das questões externas (fatores legais, tecnológicos, de mercado, sociais, econômicos, etc.)</span>
          </label>
          <button type="button" class="more" data-target="m2">Ler mais</button>
          <div id="m2" class="more-body" hidden>
          Aqui entram as condições externas que podem afetar sua empresa.
          Exemplos: novas leis, inovações tecnológicas, mudanças no mercado,
          fatores sociais (como comportamento do consumidor),
          condições econômicas e concorrência. O objetivo é identificar riscos
          e oportunidades que vêm de fora.
        </div>
        </li>
        <li>
          <label class="chk">
            <input type="checkbox" name="chk_monitoramento" class="chk-item" <%= documentos[0]?.checklist?.includes("chk_monitoramento") ? "checked" : "" %>>
            <span class="checkmark"></span>
            <span>Evidências de monitoramento e análise crítica periódica dessas questões</span>
          </label>
          <button type="button" class="more" data-target="m3">Ler mais</button>
          <div id="m3" class="more-body" hidden>
          Não basta listar os fatores, é preciso mostrar que eles são revisados periodicamente.
          Exemplos: atas de reuniões de análise crítica, relatórios de risco ou SWOT atualizada.
          Isso garante que a empresa acompanha as mudanças internas e externas de forma contínua.
        </div>
        </li>
      </ul>

      <p class="subcap-note">* O checklist é apenas um lembrete. O documento enviado deve cobrir todos os pontos.</p>  

      <div class="form-actions"> 
        <button type="submit" id="btn-salvar-checklist" class="btn-subcap-salvar">Salvar Checklist</button>     
      </div> 
    </form>
  </section>

 <!-- Upload + Ações -->
<section class="subcap-upload">
  <form action="/iso9001/<%= capitulo %>/upload" method="POST" enctype="multipart/form-data" id="form-subcap">
    <input type="hidden" name="_csrf" value="<%= csrfToken %>">

    <div class="upload-actions">
      <!-- Botão de anexo -->
      <input type="file" id="file-upload" name="ficheiros[]" multiple hidden>
      <label for="file-upload" class="btn-anexo">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16"
             viewBox="0 0 24 24" fill="none" stroke="#ffffff" stroke-width="3"
             stroke-linecap="round" stroke-linejoin="round">
          <path d="M5 12h14"/><path d="M12 5v14"/>
        </svg>
        Adicionar anexo
      </label>

      <!-- Botões de ação -->
      <div class="form-actions">
        <button type="submit" id="btn-salvar" class="btn-subcap-salvar">Salvar</button>
        <button type="button" id="btn-cancelar" class="btn-subcap-cancelar" hidden>Cancelar</button>
      </div>
    </div>

    <!-- Pré-visualização -->
    <div id="file-preview" class="file-preview vazio">
      <em>Nenhum arquivo selecionado.</em>
    </div>
  </form>

  <!-- Arquivos já anexados -->
  <% if (documentos && documentos.length > 0) { %>
    <div class="subcap-arquivos">
      <% documentos.forEach(doc => { %>
        <% if (doc.ficheiros && doc.ficheiros.length > 0) { %>
          <% doc.ficheiros.forEach(f => { %>
            <div class="file-row">
              <!-- Nome -->
              <span class="file-name"><%= f.nomePersonalizado || f.nomeOriginal %></span>

              <!-- Tamanho -->
              <span class="file-size"><%= (f.size / 1024).toFixed(2) %> KB</span>

              <!-- Aprovado por -->
              <input type="text" value="<%= f.aprovadoPor || '' %>" placeholder="Aprovado por..." readonly>

              <!-- Ações (download + excluir) -->
              <div class="file-actions">
                <!-- Botão de download -->
                <a href="/uploads/<%= f.path %>" target="_blank" class="btn-download" title="Baixar arquivo">
                  <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22"
                       viewBox="0 0 24 24" fill="none" stroke="#04b2f2"
                       stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M12 17V3"/><path d="m6 11 6 6 6-6"/><path d="M19 21H5"/>
                  </svg>
                </a>

                <!-- Botão excluir -->
                <form action="/iso9001/<%= capitulo %>/ficheiro/<%= f._id %>/apagar"
                      method="POST" style="display:inline;">
                  <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                  <button type="submit" class="btn-apagar" title="Excluir arquivo">
                    <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22"
                         viewBox="0 0 24 24" fill="none" stroke="#ff4d4d" stroke-width="2"
                         stroke-linecap="round" stroke-linejoin="round"
                         class="lucide lucide-trash-2">
                      <path d="M10 11v6"/><path d="M14 11v6"/>
                      <path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6"/>
                      <path d="M3 6h18"/><path d="M8 6V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/>
                    </svg>
                  </button>
                </form>
              </div>
            </div>
          <% }) %>
        <% } %>
      <% }) %>
    </div>
  <% } %>
</section>


</main>

<%- include('../includes/footer') %>
<script src="/assets/js/subcapitulo.js"></script>
<script src="/assets/js/script.js"></script>
</body>
</html>
